import {useForm} from "@mantine/form";
import {TextInput} from "@mantine/core";

export default function Connexion(){
    const form = useForm();

    return (
        <div>
            <TextInput/>
        </div>
    );
}